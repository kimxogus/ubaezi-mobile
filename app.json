{
  "expo": {
    "sdkVersion": "20.0.0",
    "name": "유배지",
    "slug": "ubaezi",
    "privacy": "unlisted",
    "version": "0.4.0",
    "scheme": "ubeazi",
    "android": {
      "package": "io.xogus.ubaezi",
      "versionCode": 21,
      "config": {
        "fabric": {},
        "googleSignIn": {
          "apiKey": "${androidGoogleApiKey}",
          "certificateHash": "${androidCertificateHash}"
        }
      }
    },
    "ios": {
      "bundleIdentifier": "io.xogus.ubaezi",
      "buildNumber": 21,
      "config": {
        "googleSignIn": {
          "reservedClientId": "${iosReservedClientId}"
        }
      }
    },
    "facebookScheme": "${facebookScheme}"
  }
}
